export type AdaptiveType = {
    large?: string,
    small?: string
}
export type CustomButtonType = {
    width?: AdaptiveType,
    height?: AdaptiveType,
    padding?: AdaptiveType,
    size?: AdaptiveType
}
export type CustomRateType = {
    size?: string,
    height?: string,
    right?: string
}
